import { useState, Fragment } from 'react'
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { TOASTS } from '../../constants'
import Toasts from './'

export const Toaster = (Story) => {
  const [toast, setToast] = useState(null)
  const createToast = () => {
    setToast({
      message: 'Hello?',
      type: TOASTS.INFO,
    })
  }
  return (
    <Fragment>
      <button onClick={createToast}>Toast something!</button>
      <Toasts toast={toast} />
    </Fragment>
  )
}

<Meta title="Components/Toasts/Toasts" />

# Toasts

Communicate events to the user.

<Canvas>
  <Story decorators={[Toaster]} name="Default">
    <Toasts />
  </Story>
</Canvas>
