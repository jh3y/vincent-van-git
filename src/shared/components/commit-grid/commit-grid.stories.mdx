import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import CommitGrid from './'

<Meta title="Components/CommitGrid" />

# Commit Grid

Where you draw your art for your GitHub contribution graph.

<Canvas>
  <Story name="Commits">
    <CommitGrid
      cells={new Array(350).fill().map(() => Math.floor(Math.random() * 4))}
    />
  </Story>
  <Story name="No commits">
    <CommitGrid />
  </Story>
</Canvas>
